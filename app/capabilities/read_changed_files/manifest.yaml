name: ReadChangedFiles
description: Read the files changed by a repository pull request.
category: repository
contract: ReadChangedFiles
version: "1"
inputs:
  - name: repository
    description: Repository identifier.
  - name: pull_request_number
    description: Pull-request number.
outputs:
  - name: changed_files
    description: Provider-neutral changed-file data.
tags:
  - repository
  - pull-request
  - files
  - read
entrypoint: app.capabilities.read_changed_files.implementation
