name: ReadPullRequest
description: Read the metadata and current state of a repository pull request.
category: repository
contract: ReadPullRequest
version: "1"
inputs:
  - name: repository
    description: Repository identifier.
  - name: pull_request_number
    description: Pull-request number.
outputs:
  - name: pull_request
    description: Provider-neutral pull-request data.
tags:
  - repository
  - pull-request
  - read
entrypoint: app.capabilities.read_pull_request.implementation
